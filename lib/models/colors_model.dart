import 'package:flutter/material.dart';

class ColorsModel {
  String svgPath;
  Color color;
  Gradient gradient;

  ColorsModel({
    required this.svgPath,
    required this.color,
    required this.gradient,
  });
}
