import 'package:device_frame/device_frame.dart';
import 'package:flutter/material.dart';

class DevicesModel {
  DeviceInfo device;
  IconData icon;

  DevicesModel({required this.device, required this.icon});
}